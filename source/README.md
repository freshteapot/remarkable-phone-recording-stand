# Parametric source

This directory is a nurb project. Install nurb, then run:

```sh
nurb dev
```

Open the URL printed by the command and select `mobile_recording_stand` for the complete assembly.

Part dimensions are keyword defaults in the Python functions. Read the matching Markdown card before changing a part: its `Don't` section records failed physical tests and constraints that are not obvious from the geometry alone.

